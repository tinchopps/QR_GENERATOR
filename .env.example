###############################################
# QR Generator Monorepo - Environment Example #
###############################################
# Copy this file to `.env` (root) or into each workspace as needed.
# Root .env will be loaded by most hosting providers or docker compose setups.

# ------------------- Backend -------------------
# Port where Express listens (default 4000)
PORT=4000

# CORS allowed origin. In development we allow '*' but for production set your domain, e.g.:
# ORIGIN=https://qr.midominio.com
ORIGIN=*

# Disable Helmet Content Security Policy (NOT recommended in prod). Set to 1 only for debugging.
# DISABLE_CSP=1

# Enable static serving of frontend build from backend container. Used in single-container deploys.
# Set to 1 after running the production build (frontend/dist available inside image).
# SERVE_STATIC=1

# Optional commit hash injected at build time for /version endpoint.
# COMMIT_HASH=abcdef1

# Rate limiting tuning (defaults are fine for demo)
# RATE_POINTS=50
# RATE_DURATION=60

# ------------------- Frontend -------------------
# When deploying frontend separately from backend, set base URL so it can call the API.
# Example if backend lives at https://api.midominio.com
# VITE_API_BASE=https://api.midominio.com

# If serving both together behind same origin (SERVE_STATIC=1), leave VITE_API_BASE blank.

# ------------------- Shared / Misc --------------
# Node environment; can influence logging verbosity or future conditionals.
# NODE_ENV=production

# Logging level for pino logger (fatal,error,warn,info,debug,trace,silent)
# LOG_LEVEL=info

# ------------------- Notes ----------------------
# 1. Do NOT commit a filled `.env` with secrets.
# 2. For Docker builds you can pass build args for COMMIT_HASH and use an --env-file.
# 3. For CI, inject only needed vars (ORIGIN, SERVE_STATIC, etc.).
# Backend environment variables
PORT=4000
RATE_LIMIT=30
ORIGIN=*
NODE_ENV=development
